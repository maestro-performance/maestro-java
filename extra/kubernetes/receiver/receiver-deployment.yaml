apiVersion: apps/v1
kind: Deployment
metadata:
  name: maestro-receiver
spec:
  replicas: 0
  selector:
    matchLabels:
      component: receiver
      app: maestro
  template:
    metadata:
      labels:
        component: receiver
        app: maestro
    spec:
      affinity:
        nodeAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 1
              preference:
                matchExpressions:
                  - key: nodetype
                    operator: In
                    values:
                      - maestroperf
      containers:
        - name: receiver
          image: maestroperf/maestro-receiver:1.3.0
          env:
            - name: MAESTRO_BROKER
              value: mqtt://broker.svc
          volumeMounts:
            - mountPath: /maestro/receiver/logs
              name: logging-volume
      volumes:
        - name: logging-volume
          emptyDir: {}
